import { retalappColor } from "./packages/pack3";
import {retalappContainer } from "./packages/pack2"
import {retalappShadow} from "./packages/pack1"

retalappColor({
    color: "verde"
})

retalappContainer()

retalappShadow({
    shadow_type: "soft",
    padding: true
})
